apiVersion: external-secrets.io/v1 # 🚀 Updated from v1beta1 to v1 stable
kind: ExternalSecret
metadata:
  name: auth-service-secrets
  namespace: issue-app
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: SecretStore
  target:
    name: auth-service-secrets
    creationPolicy: Owner
  data:
    - secretKey: SPRING_DATASOURCE_USERNAME
      remoteRef:
        key: prod/issue/db
        property: username
    - secretKey: SPRING_DATASOURCE_PASSWORD
      remoteRef:
        key: prod/issue/db
        property: password
    - secretKey: JWT_SECRET
      remoteRef:
        key: prod/game/jwt
        property: secret
    # If you aren't using live SES keys yet, you can map temporary placeholder values from your DB secret keys
    - secretKey: SES_USERNAME
      remoteRef:
        key: prod/issue/db
        property: username
    - secretKey: SES_PASSWORD
      remoteRef:
        key: prod/issue/db
        property: password