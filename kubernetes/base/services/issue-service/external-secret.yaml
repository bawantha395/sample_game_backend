apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: issue-service-secrets
  namespace: issue-app
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: SecretStore
  target:
    name: issue-service-secrets # This automatically creates the missing secret container!
    creationPolicy: Owner
  data:
    - secretKey: SPRING_DATASOURCE_USERNAME
      remoteRef:
        key: prod/issue/db
        property: username
    - secretKey: SPRING_DATASOURCE_PASSWORD
      remoteRef:
        key: prod/issue/db
        property: password
    - secretKey: JWT_SECRET
      remoteRef:
        key: prod/game/jwt
        property: secret